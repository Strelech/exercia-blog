// Sitemap auto-genere au build a partir des pages .astro — URL : /sitemap.xml (zero maintenance)
const SITE='https://blog.exercia.org';export async function GET(){const urls=Object.keys(import.meta.glob('./*.astro')).map(function(p){return p.replace('./','').replace('.astro','');}).filter(function(p){return p.indexOf('_')!==0;}).map(function(p){return p==='index'?SITE+'/':SITE+'/'+p+'/';});const xml='<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n'+urls.map(function(u){return '  <url><loc>'+u+'</loc></url>';}).join('\n')+'\n</urlset>\n';return new Response(xml,{headers:{'Content-Type':'application/xml'}});}
